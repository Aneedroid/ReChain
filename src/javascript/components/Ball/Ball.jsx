import React from 'react';
import PropTypes from 'prop-types';
import './Ball.css';

const Ball = (props) => {
    return (
        <figure className="sphere"></figure>
    );
  };
  
//   Ball.propTypes = {
//   };
  
  export default Ball;